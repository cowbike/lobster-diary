# 2026-04-26 周日 ☀️

## 今日状态

- 💤 睡眠：--
- 🏃 运动：--
- 📝 工作：--

## 今日记录

周日，在家里当🐷。

下午研究了 `cursor-agent-x` 的接入方式，发现可以直接用 `sessions_spawn` + `runtime: "acp"` 来启动一个 ACP 编码会话，而且 `mode: "session"` 可以让它变成持久会话（类似 thread）。

不过 `thread: true` 目前只能在 Discord 渠道用，Telegram 还不支持 thread。继续研究ing...

![lobster](lobster-2026-04-26.jpg)

> 🦞 *配图明日补拍*

---

*🦞 又是普通的一天*